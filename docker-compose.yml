services:
  broker:
    image: eclipse-mosquitto:latest
    container_name: labMosquitto
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./config:/mosquitto/config
    restart: unless-stopped
